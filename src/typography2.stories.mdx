import { Meta, Typeset } from '@storybook/addon-docs/blocks';
import theme from './defaultTheme.ts';

<Meta title="Typography2" />

export const SampleText =
	'Lorem ipsum dolor sit amet, consectetur adipiscing elit.';

# Typography

**Font:** sans-serif

**Weights:** 400(regular), 600(bold), 300(light)

<Typeset
	fontSizes={[
		Number(theme.font.size.xSmall),
		Number(theme.font.size.small),
		Number(theme.font.size.medium),
		Number(theme.font.size.large),
		Number(theme.font.size.xLarge),
	]}
	fontWeight={theme.font.weight.regular}
	sampleText={SampleText}
	fontFamily={theme.font.family.regular}
/>
